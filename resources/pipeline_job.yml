resources:
  jobs:
    elio_pipeline:
      name: elio_pipeline
      description: Olist medallion pipeline (setup -> bronze -> silver -> gold)
      max_concurrent_runs: 1
      timeout_seconds: 3600
      email_notifications:
        on_failure:
          - ${workspace.current_user.userName}
      parameters:
        - name: catalog
          default: ${var.catalog}
      tasks:
        - task_key: setup
          notebook_task:
            notebook_path: ../pipeline/00_setup.sql

        - task_key: ingest_bronze
          depends_on:
            - task_key: setup
          notebook_task:
            notebook_path: ../ingest/load_bronze.py

        - task_key: create_tables_silver
          depends_on:
            - task_key: ingest_bronze
          notebook_task:
            notebook_path: ../pipeline/01_create_tables.sql

        - task_key: bronze_to_silver
          depends_on:
            - task_key: create_tables_silver
          notebook_task:
            notebook_path: ../pipeline/02_bronze_to_silver.py

